<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DashBoard</title>
    <!-- icons -->
    <!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"> -->
    <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN" crossorigin="anonymous">
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
        
    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>

    <!-- Latest compiled JavaScript -->
	<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
	<style>
        .card {
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            max-width: 300px;
            margin: 50px auto;
            text-align: center;
            font-family: arial;
        }
        .email {
            color: grey;
            font-size: 18px;
        }
        button {
            border: none;
            outline: 0;
            display: inline-block;
            padding: 8px;
            color: white;
            background-color: #000;
            text-align: center;
            cursor: pointer;
            width: 100%;
            font-size: 18px;
        }
        a {
            text-decoration: none;
            /* font-size: 22px; */
            color: white;
            /* color: black; */
        }
        button:hover {
            opacity: 0.7;
        }
        #count{
            display: flex;
            justify-content: space-evenly;
        }
        .articleContainer {
			width: 450px;
			/* border: aqua 1px solid; */
			min-height: 50px;
			margin: 30px auto 30px;
			background-color: rgba(40, 40, 40, 0.5);
			padding-left: 20px;
			padding-right: 20px;
		}
		.articleContainer:hover {
			background-color: rgba(40, 40, 40, 0.2);
			cursor: pointer;
		}
		h3{
			text-align: center;
		}
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="card">
        <h1><%= user.name%></h1>
        <p class="email"><%= user.email %></p>
        <p>Age: <%= user.age%></p>
        <p>TotalArticle: <%= user.totalArticle %></p>
        <div id="count">
            <p>Followers: <br><%= user.followersCount%></p>
            <p>Following:  <br><%= user.followingCount%></p>
        </div>
        <a href="/user/update"><button>Want To Update?</button></a>
    </div>

    <div class="container">
      	<h2 style="text-align: center;" >Articles</h2>
      	<% for(let i=0 ; i < user.article.length; i++ ) {%>
			<div class="articleContainer" onclick="window.location.href = '/article/view/<%= user.article[i]._id %>';">
				<h3><%= user.article[i].title %></h3>
			</div>
      	<% } %>
    </div>
</body>
</html>